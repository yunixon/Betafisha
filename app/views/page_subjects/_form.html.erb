<%= form_for(@page_subject) do |f| %>
  <% if @page_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_subject.errors.count, "error") %> prohibited this page_subject from being saved:</h2>

      <ul>
      <% @page_subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
