<% title "Форумы" %>

<table>
  <tr>
    <th>Форум</th>
    <th>Описание</th>
    <th>Последние обновление</th>
    <% if current_user.admin? %>
      <th>Администрирование</th>
    <% end %>
  </tr>
    <% for forum in @forums %>
      <tr>
        <td><%= link_to forum.name, forum %> (<%= forum.topics.count %>)</td>
        <td><%= forum.description %></td>
        <td><%= forum.topics.last.created_at > 1.days.ago ? image_tag("http://www.awwwards.com/images/ico_pending.png") + " " + time_ago_in_words(forum.topics.last.created_at) + t( :ago ) : l(forum.topics.last.created_at, :format => :long) if !forum.topics.last.nil? %> </td>
 
        <% if current_user.admin? %>
          <td><%= link_to "Редактировать", edit_forum_path(forum) %> <%= link_to "Удалить", forum, :confirm => 'Вы уверены, что хотите удалить форум?', :method => :delete %></td>
        <% end %>

      </tr>
    <% end %>
</table>

<p><%= link_to "Добавить форум", new_forum_path %></p>
